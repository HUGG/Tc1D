# Notes

## Design

1. Starting point notebook with general info about notebooks, Binder, and 1-2 simple models. (`getting_started.ipynb`)

    - Binder would automatically launch this one
    - Links here would point to the other notebooks
    - List of all model parameters and how to produce that list (e.g., `help(tc1d.init_params)`?

2. Forward model examples notebook showing a handful of different functionalities (model parameters and cool things you can do) (`forward_mode.ipynb`)
3. Inverse model examples (`inverse_mode.ipynb`)
4. Different ways to run Tc1D (`ways_to_run_tc1d.ipynb`)

    - Include link to YAML input file here

6. Notebooks for reproducing results (named based on the article, for example; subdirectory?)
7. Notebook(s) for teaching?